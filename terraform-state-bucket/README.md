# Terraform State Bucket

This module creates a bucket for storing Terraform state for all the other modules.

Because it creates the bucket, I can't actually store the state for this module in the bucket, so it is in the repo.
